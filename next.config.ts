import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com', 'randomuser.me', 'media.istockphoto.com'],
  },
};

export default nextConfig;
